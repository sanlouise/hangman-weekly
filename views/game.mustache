<!DOCTYPE html>
<html>
  <head>
    <meta charset="utf-8">
    <title>Hangman</title>
    <link href="https://fonts.googleapis.com/css?family=Josefin+Sans" rel="stylesheet">
    <link rel="stylesheet" type="text/css" href="styles.css">
    <link rel="hangman icon" href="hangman.png" />
  </head>
  <body>

    <div class="game">
      <h1>Lose and You'll Die</h1>
      <div class="row">
        {{#keyboardRow1}}
          <form action="/attempt?key={{.}}" method="post">
            <button id="{{.}}" class="key" type="submit">{{.}}</button>
          </form>
        {{/keyboardRow1}}
      </div>
      <div class="row">
        {{#keyboardRow2}}
          <form action="/attempt?key={{.}}" method="post">
            <button id="{{.}}" class="key" type="submit">{{.}}</button>
          </form>
        {{/keyboardRow2}}
      </div>
      <div class="row">
        {{#keyboardRow3}}
          <form action="/attempt?key={{.}}" method="post">
            <button id="{{.}}" class="key" type="submit">{{.}}</button>
          </form>
        {{/keyboardRow3}}
      </div>

      <div class="hidden-word">
        <h3>{{hiddenWord}}</h3>
      </div>

      <div class="info-and-img">
        <div class="hangman-img-{{badAttemptCounter}}">
        </div>
        <div class="game-info">
          <span>Guessed letters:
            {{#attemptedLettersArray}}
              {{.}}
            {{/attemptedLettersArray}}
          </span>
          <br>
          {{displayedMessage}}
          <br>
          {{getRemainingAttempts}}
        </div>

    </div>
  </body>
</html>
